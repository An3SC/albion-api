import React from 'react';
import Events from './Events';
import Players from './Players';

const Home = () => {
    return (
        <div>
            <Events />
            <Players />
        </div>
    );
};

export default Home;
