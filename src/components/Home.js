import React from 'react';
import Events from './Events';
import Players from './Players';

const Home = () => {
    return (
        <div>
            <Players />
            <Events />
        </div>
    );
};

export default Home;
