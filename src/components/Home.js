import React from 'react';
import Events from './Events';
import Player from './Player';

const Home = () => {
    return (
        <div>
            <Events />
            <Player />
        </div>
    );
};

export default Home;
